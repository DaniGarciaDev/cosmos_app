---
const API_KEY = "DEMO_KEY"; 
const response = await fetch(`https://api.nasa.gov/planetary/apod?api_key=${API_KEY}`);
const data = await response.json();
import Layout from "../../layouts/Layout.astro";
import "../../styles/global.css";
---

<html lang="es">
  <head>
    <title>NASA APOD | Cosmos App</title>
  </head>
  <Layout>
    <section class="content">
      <h2>📷 Imagen Astronómica del Día</h2>
      <h3>{data.title}</h3>
      <p><em>{data.date}</em></p>
      {data.media_type === "image" ? (
        <img src={data.url} alt={data.title} style="max-width: 100%; border-radius: 12px; margin: 2rem 0;" />
      ) : (
        <iframe width="100%" height="400" src={data.url} frameborder="0" allowfullscreen></iframe>
      )}
      <p>{data.explanation}</p>
    </section>
  </Layout>
</html>
